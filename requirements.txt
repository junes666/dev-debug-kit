# 必需依赖（联网装一次，之后全离线运行）
PySide6>=6.5
quickjs>=1.19.4
segno>=1.5
pycryptodome>=3.19

# 可选：二维码解析（不装则解析功能提示安装，其余功能不受影响）
zxing-cpp>=2.2
Pillow>=9.0

# 可选：离线翻译（需配合 models/ 神经翻译模型；不装则翻译页提示，其余不受影响）
ctranslate2>=4.0
sentencepiece>=0.2
